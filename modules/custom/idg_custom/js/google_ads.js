/////////////Add your Js here/////////////////
(function($, Drupal, drupalSettings) {
			

    // can access setting from 'drupalSettings';
    var lotusHeight = drupalSettings.variable;
     alert('jhggh');

  })(jQuery, Drupal, drupalSettings);

